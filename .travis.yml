---
language: "python"
python:
  - "3.7"

# Install python packages
install:
  - "pip install -r requirements.txt"

# Perform pre-checks
before_script:
  - "make lint"

# Execute script on specified inputs
script:
  - "make run"
...
